[metadata]
name = hmxlabs.sysinfo
version = 1.0.3

[options]
packages = find_namespace:
package_dir = 
    = src

[options.packages.find]  
where=src
include=hmxlabs*  # * by default
exclude=tests*  # empty by default